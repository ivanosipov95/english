import { ConfirmByEnterDirective } from './confirm-by-enter.directive';

describe('ConfirmByEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmByEnterDirective();
    expect(directive).toBeTruthy();
  });
});
